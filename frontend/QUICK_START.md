# Быстрый старт

## ⚠️ Важно: Сначала установите Node.js!

Если вы видите ошибку "npm не распознано", сначала установите Node.js:
1. Скачайте с https://nodejs.org/ (LTS версию)
2. Установите, следуя инструкциям
3. **Закройте и откройте заново** PowerShell
4. Проверьте: `node --version` и `npm --version`

## После установки Node.js

### 1. Установите зависимости

```powershell
npm install
```

### 2. Создайте файл .env.local

Создайте файл `.env.local` в корне проекта со следующим содержимым:

```env
NEXT_PUBLIC_API_URL=http://localhost:8000/api
```

### 3. Запустите Backend (в отдельном терминале)

```powershell
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python create_admin.py
python init_data.py
uvicorn app.main:app --reload --port 8000
```

### 4. Запустите Frontend

В корне проекта:

```powershell
npm run dev
```

### 5. Откройте в браузере

- Frontend: http://localhost:3000
- Backend API: http://localhost:8000
- API Docs: http://localhost:8000/docs

## Готово! 🎉

Теперь вы можете:
- Просматривать меню
- Бронировать столы
- Оставлять отзывы
- Войти как администратор (admin/admin123)

