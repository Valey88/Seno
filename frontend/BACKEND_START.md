# Команды для запуска Backend

## Шаг 1: Перейти в папку backend

```powershell
cd backend
```

## Шаг 2: Создать виртуальное окружение (если еще не создано)

```powershell
python -m venv venv
```

## Шаг 3: Активировать виртуальное окружение

```powershell
venv\Scripts\activate
```

После активации вы увидите `(venv)` в начале строки PowerShell.

## Шаг 4: Установить зависимости

```powershell
pip install -r requirements.txt
```

## Шаг 5: Создать администратора (первый раз)

```powershell
python create_admin.py
```

Это создаст пользователя:
- **Username:** admin
- **Password:** admin123

## Шаг 6: Загрузить начальные данные (первый раз)

```powershell
python init_data.py
```

Это создаст:
- Столы (13 столов в 3 залах)
- Категории меню (8 категорий)
- Позиции меню (10 блюд)

## Шаг 7: Запустить сервер

```powershell
uvicorn app.main:app --reload --port 8000
```

## Готово! 🎉

Backend будет доступен:
- **API:** http://localhost:8000
- **Документация:** http://localhost:8000/docs
- **Альтернативная документация:** http://localhost:8000/redoc

## Все команды одной строкой (для копирования)

```powershell
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python create_admin.py
python init_data.py
uvicorn app.main:app --reload --port 8000
```

## Примечания

- `--reload` - автоматическая перезагрузка при изменении кода (для разработки)
- `--port 8000` - порт, на котором будет работать сервер
- Если порт 8000 занят, используйте другой: `--port 8001`

## Остановка сервера

Нажмите `Ctrl + C` в терминале, где запущен сервер.

## Проверка работы

Откройте в браузере: http://localhost:8000/docs

Вы должны увидеть интерактивную документацию API (Swagger UI).

